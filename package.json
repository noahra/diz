{
  "name": "diz",
  "version": "0.1.0",
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile diz",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
